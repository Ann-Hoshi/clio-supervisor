import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CLIO_supervisor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
